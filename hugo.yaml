baseURL: 'https://notes.awendland.co.uk/'
languageCode: 'en-gb'
title: "Alex's public notes"
contentDir: "notes"
theme:
  - personal

markup:
  goldmark:
    extensions:
      passthrough:
        delimiters:
          block:
            - - $$
              - $$
          inline:
            - - $
              - $
        enable: true
    renderHooks:
      link:
        useEmbedded: fallback

params:
  math: true

caches:
  images:
    dir: :cacheDir/images
